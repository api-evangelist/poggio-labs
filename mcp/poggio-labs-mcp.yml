generated: '2026-07-20'
method: searched
status: published
source: https://poggio.io/docs/mcp
server:
  name: poggio
  transport: http
  url: https://mcp.poggio.io/mcp
  version: 1.0.05
authentication:
  methods:
  - oauth-dynamic-client-registration
  - oauth-authorization-code
  - oauth-client-credentials
  - bearer-token
  authorization_server: https://api.poggio.io/
docs:
  connecting: https://poggio.io/docs/mcp
  tool_specs: https://poggio.io/docs/mcp/tool-specs
  extending: https://poggio.io/docs/developer/extending-mcp
tools:
- name: search
  description: Returns a list of account intelligence documents for the supplied query.
  parameters:
  - name: query
    type: string
    description: Domain name of the target company.
- name: fetch
  description: Returns the full text of the requested account intelligence document.
  parameters:
  - name: id
    type: string
    description: Document ID from search results.
- name: create_account
  description: Establishes a new account in your Poggio workspace with immediate intelligence gathering.
  parameters:
  - name: account_domain
    type: string
    description: Company domain name.
- name: recreate_account_pages
  description: Recreates all pages for an existing account using current default templates.
  parameters:
  - name: account_domain
    type: string
    description: Company domain name.
- name: ask_poggio
  description: Leverages Poggio's AI assistant with access to web search, Salesforce, Gong transcripts,
    and company intelligence for comprehensive research.
  parameters:
  - name: question
    type: string
    description: Research query or analysis request.
deployment:
  mode: remote
  endpoint: https://mcp.poggio.io/mcp
  auth: oauth
  verified: probed
  probe: gated
  checked: '2026-08-13'
  source: catalog MCP census; re-probed 2026-08-13
  evidence:
    request: POST https://mcp.poggio.io/mcp {"jsonrpc":"2.0","id":1,"method":"tools/list"}
    http_status: 401
    error: invalid_token
    www_authenticate: >-
      Bearer error="invalid_token", resource_metadata="https://mcp.poggio.io/.well-known/oauth-protected-resource/mcp"
    server: uvicorn
    note: >-
      The live tool schemas are auth-gated. The tools[] list above is the provider's own
      published catalog from https://poggio.io/docs/mcp/tool-specs — names, descriptions and
      parameters as documented; full inputSchemas would require authenticated introspection.
